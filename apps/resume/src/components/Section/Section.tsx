import { styled } from '@nextui-org/react';

const section: any = styled('section', {
    marginTop: '3rem',
    '& > div:first-of-type': {
        marginTop: '2rem',
    },
});

export default section;
